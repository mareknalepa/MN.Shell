<UserControl x:Class="MN.Shell.Framework.MessageBox.MessageBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MN.Shell.Framework.MessageBox"
             xmlns:controls="clr-namespace:MN.Shell.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="160" d:DesignWidth="320"
             d:DataContext="{d:DesignInstance local:DtMessageBoxViewModel, IsDesignTimeCreatable=True}">
    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ContentControl Grid.Row="0"
                        Content="{Binding Message}"
                        Margin="10"
                        Foreground="{DynamicResource ActiveForegroundBrush}"
                        HorizontalAlignment="Center"/>
        <controls:DialogButtons Grid.Row="1" ItemsSource="{Binding Buttons}"/>
    </Grid>
</UserControl>
